name: 'Pull Request Action: Test & Lint'

on:
  pull_request:
    branches:
      - master

permissions:
  id-token: write
  contents: read
  pull-requests: write

jobs:
  test-and-lint:
    uses: ./.github/workflows/test-and-lint.yml
